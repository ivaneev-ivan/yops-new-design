import React from 'react';
import { Text } from '@mantine/core';

const YopsVpn = () => (
    <Text component="span" variant="gradient" gradient={{ from: 'blue', to: 'cyan' }} inherit>
      Yops VPN
    </Text>);

export default YopsVpn;
